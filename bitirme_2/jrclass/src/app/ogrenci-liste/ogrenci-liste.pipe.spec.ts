import { OgrenciListePipe } from './ogrenci-liste.pipe';

describe('OgrenciListePipe', () => {
  it('create an instance', () => {
    const pipe = new OgrenciListePipe();
    expect(pipe).toBeTruthy();
  });
});
